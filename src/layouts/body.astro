---
// Script For Title.
const { pathname } = Astro.url;

const routes: Record<string, string> = {
  "/": "Welcome",
  "/featured": "Featured",
  "/whats-new": "Release Logs",
  "/store": "Store",
  "/settings": "Settings",
  "/info": "Info",
  "/watch": "Watching",
};

const title = "YokaiVerse | " + (routes[pathname] ?? "404: Not Found");

import "../styles/global.css";

import Footer from "../components/footer.astro";
import LoadingAnimation from "../components/custom/loadingAnimation.astro";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{title}</title>
    <link rel="icon" href="/favicon.svg" type="image/png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />
  </head>
  <body
    class="relative flex flex-col gap-2 bg-Light_Background dark:bg-Dark_Background font-Sans caret-Accent accent-Accent selection:bg-Accent/45 overflow-hidden overflow-y-scroll select-text scroll-smooth! custom-scrollbar"
  >
    <LoadingAnimation />
    <slot />
    <Footer givenTitle={title} />
    <script>
      import "../scripts/languageHandler.ts"; // Language Handler
      import "../scripts/svgScripts.ts";
      import "../scripts/watchPage_Scripts/videoPlayer.ts"; // for anything that handles video Player
      import "../scripts/watchPage_Scripts/playerControls.ts"; // for the video player controlls
      import "../scripts/watchPage_Scripts/playlistHandler.ts"; // for the playlist handles and page.
      import "../scripts/infoPage_Scripts/infoHandler.ts"; // for the info handles and page.
      import "../scripts/animeListerScript"; // handles the load on featured.
    </script>
  </body>
</html>
