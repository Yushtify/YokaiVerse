---
import NavigationLinkButton from "./custom/buttons/navigationHorizontal.astro";
import NavigationHorizontalButton from "./custom/buttons/navigationHorizontalButton.astro";
---

<div
    class="sticky top-0 flex-row items-center justify-center p-2 w-screen h-fit z-999"
>
    <div
        class="flex flex-row items-center justify-center sm:justify-between p-2 rounded-3xl gap-2 w-full h-fit bg-L_Primary/64 dark:bg-D_Primary/64 backdrop-blur-3xl"
    >
        <!-- Left Elements -->
        <div
            class="flex flex-row items-center justify-center gap-2 min-w-fit h-fit"
        >
            <a
                href="/"
                class="flex flex-row items-center justify-center w-fit max-h-full h-full aspect-square gap-2 overflow-hidden"
            >
                <img
                    src="/logo.svg"
                    alt="YOKAIVERSE LOGO"
                    class="h-12 aspect-square dark:hidden"
                />
                <img
                    src="/logo_d.svg"
                    alt="YOKAIVERSE LOGO"
                    class="h-12 aspect-square hidden dark:inline"
                />
            </a>
            <NavigationLinkButton
                givenLocationFrom="horizontal"
                givenLocation="start"
                givenSymbol="home"
                givenHref="/"
                givenTitle="home"
                givenData="Navbar.home"
            />
            <NavigationLinkButton
                givenLocationFrom="horizontal"
                givenLocation="center"
                givenSymbol="star"
                givenHref="/featured"
                givenTitle="featured"
                givenData="Navbar.featured"
            />
            <NavigationLinkButton
                givenLocationFrom="horizontal"
                givenLocation="end"
                givenSymbol="explore"
                givenHref="/discover"
                givenTitle="discover"
                givenData="Navbar.discover"
            />
        </div>
        <!-- Middle Elements -->
        <div
            class="hidden md:flex flex-row items-center justify-start gap-4 w-full h-fit"
        >
            <NavigationHorizontalButton
                givenAction="startSearching()"
                givenSymbol="search"
                givenSymbolSize="text-[min(6vw,1rem)]"
            />
        </div>
        <!-- Right Elements -->
        <div
            class="flex flex-row items-center justify-center gap-4 min-w-fit h-fit"
        >
            <NavigationLinkButton
                givenSymbol="settings"
                givenHref="/settings"
            />
        </div>
    </div>
</div>
